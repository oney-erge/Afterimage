#!/usr/bin/env bash
# One command, any platform (macOS/Linux/WSL2): ./start
# First run sets things up; every run after that just starts the server.
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")"
exec ./run.sh "$@"
