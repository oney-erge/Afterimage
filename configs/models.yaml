# Target model architecture dimensions.
#
# IMPLEMENTATION_PLAN.md #0: "Record actual architecture dimensions from the
# checkpoint... The sketch-size arithmetic in HYPOTHESIS.md #3.6 is currently
# derived from assumed dimensions and must be recomputed from real config
# before anyone quotes it."
#
# The values below are PLACEHOLDERS copied from public model cards, not
# verified against a downloaded checkpoint -- this environment has no
# `transformers` package and no downloaded weights (IMPLEMENTATION_STATUS.md).
# Replace every field marked UNVERIFIED before running Phase 0 for real.

gemma-3-27b:
  status: UNVERIFIED
  d_model: 5376
  d_ffn: 21504
  n_layers: 62
  n_kv_heads: 8
  quantization: Q4_K_M
  approx_size_gb: 16.0

qwen3-32b:
  status: UNVERIFIED
  d_model: 5120
  d_ffn: 25600  # SwiGLU gate+up+down triple -- confirm against config.json, not assumed
  n_layers: 64
  n_kv_heads: 8
  quantization: Q4
  approx_size_gb: 19.0

external-draft-candidates:
  - llama-3.2-1b
  - qwen3-0.6b

proxy-model-for-fast-iteration:
  name: qwen3-1.7b
  note: "used for all fast iteration per IMPLEMENTATION_PLAN.md #0; final Phase 0 / Phase 6 numbers must use the full-size targets above"
